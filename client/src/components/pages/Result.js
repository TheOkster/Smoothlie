import React from "react";

const Result = (props) => {
  //   const newSmoothie = get("api/scheduler", { schedule: props.schedule, tasks: props.tasks });
  const body = {}; // TODO: fill in
  //   post("/smoothie", body);

  return <>this is the results page</>;
};

export default Result;
